## 滚动条样式修改

```scss
@mixin transparentScrollBar {
  &::-webkit-scrollbar-track-piece {
    background: transparent;
  }
  &::-webkit-scrollbar {
    width: 6px;
  }
  &::-webkit-scrollbar-thumb {
    background: #AFAFAF;
    border-radius: 20px;
  }
}
```
